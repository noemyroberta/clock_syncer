syntax = "proto3";

package clock;

service ClockSync {
    rpc Sync (SyncRequest) returns (SyncResponse) {}
}

message SyncRequest {
    int64 client_time = 1;
}

message SyncResponse {
    int64 server_time = 1;
}
